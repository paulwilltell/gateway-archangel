# Vendored dependencies

## loom_engine.py

Pinned copy of **Loom v2.2** (`engine.py`) from the Loom project
(`C:\Users\pault\loom`, github.com/paulwilltell/loom, private). Copied
2026-07-26 with Loom's full gate (`run_tests.py`) green.

Loom is the deterministic, auditable reasoning engine that verifies Archangel
analyses (see `app/loom_bridge.py`). Do not edit this file here — improve
Loom upstream, run its gate, then re-copy deliberately.
