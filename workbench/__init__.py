"""The Workbench — a personal Bible-study instrument.

Separate from Gateway on purpose. Gateway is a trust engine: it refuses to
claim more than the text supports, because strangers rely on it. The Workbench
is a discovery instrument for one honest user who knows he is the validator —
it *generates* candidates (echoes, anomalies, connections) so a human can dig.

The one rule that keeps it from becoming a delusion generator: **every pattern
it surfaces arrives with a null test.** These methods never fail to produce
impressive-looking output; the null baseline is the only thing that separates
a real signal from an artifact you can talk yourself into. Nothing here is
shown without "…and here is whether it survives chance."

There is a one-way door to Gateway: anything the Workbench makes you curious
about, you take through Gateway's trust pipeline (Loom, the counterpassage
index, the published hermeneutic) to test whether it holds. Generate divergent
here; validate convergent there.
"""
